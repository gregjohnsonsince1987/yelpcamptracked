<%- include("partials/header") %>

<!-- Looping through each of the campgrounds in our array -->
<% campgrounds.forEach(function(campground){  %>
    <div>
        <h4><%= campground.name %></h4>
        <img src="<%= campground.image %>">
    </div>
    

<% }); %>


<%- include("partials/footer") %>